program Project1;

{$APPTYPE CONSOLE}

uses
  SysUtils;

var
a:word;
b:longint;

begin
read(a);

asm
MOV ECX, 0
MOV CX, a
MOV b,ECX
end;

writeln(' ');
writeln(b);

readln; readln;
end.
